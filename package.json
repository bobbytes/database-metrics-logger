{
  "name": "cf-service-metrics-logger",
  "version": "0.0.3",
  "description": "Log Cloud Foundry service metrics",
  "main": "dist/index.js",
  "module": "dist/index.es.js",
  "files": [
    "dist"
  ],
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rollup -c",
    "test": "set NODE_ENV=test&& jest --env=jsdom --coverage",
    "lint": "tslint --project tsconfig.json --format stylish",
    "prebuild": "npm run lint && rimraf dist && rimraf .tmp",
    "postbuild": "npm run bundle && rimraf .tmp",
    "bundle": "dts-bundle --name dist/index --main .tmp --outputAsModuleFolder"
  },
  "keywords": [
    "typescript",
    "cloudfoundry",
    "service",
    "metrics",
    "logger"
  ],
  "engines": {
    "node": ">=8.x"
  },
  "jest": {
    "transform": {
      "^.+\\.(js|ts|tsx)$": "<rootDir>/node_modules/ts-jest/preprocessor.js"
    },
    "testRegex": "(/__tests__/.*|\\.(test|spec))\\.(ts|tsx|js)$",
    "testPathIgnorePatterns": [
      "<rootDir>/(node_modules|dist)"
    ],
    "coverageDirectory": "<rootDir>/test/coverage",
    "collectCoverageFrom": [
      "src/**/*.{t,j}s?(x)",
      "!src/**/index.ts",
      "!src/**/*.d.ts"
    ],
    "moduleFileExtensions": [
      "js",
      "jsx",
      "json",
      "ts",
      "tsx"
    ]
  },
  "contributors": [
    {
      "name": "Robert Kleger",
      "email": "robert.kleger@bobb.io"
    }
  ],
  "repository": "git+ssh://git@github.com:DaNautilus/cf-service-metrics-logger.git",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^23.3.13",
    "@types/mongodb": "^3.1.19",
    "@types/redis": "^2.8.10",
    "coveralls": "^3.0.2",
    "dts-bundle": "^0.7.3",
    "jest": "^23.6.0",
    "mongo-unit": "^1.3.4",
    "rollup": "^1.1.2",
    "rollup-plugin-node-globals": "^1.4.0",
    "rollup-plugin-typescript2": "^0.19.2",
    "ts-jest": "^23.10.5",
    "tslint": "^5.12.1",
    "typescript": "^3.2.4"
  },
  "dependencies": {
    "cfenv": "^1.1.0",
    "mongodb": "^3.1.13",
    "redis": "^2.8.0"
  }
}
